"use client";

import { useOnboarding } from "../context/onboarding-context";

// Re-export the hook from the context file for cleaner imports if prefered, 
// or keep this file as the main entry point for the hook. 
// Since I defined the hook in the context file for simplicity, I will re-export it here.

export { useOnboarding };
